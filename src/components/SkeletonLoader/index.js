// SkeletonLoader.js
import React from 'react';
import './index.css'; // Import the CSS for the skeleton loader

const SkeletonLoader = () => (
  <div className="skeleton-loader"></div>
);

export default SkeletonLoader;
